using System;

namespace Csharp {
    class Program {
        static void Main(string[] args) {
            // Tipos de variaveis

            // Tipos com números
            int inteiro = 12;
            double meuDouble = 12.00D;

            // Tipos com caracteres
            char caractere = 'J';
            char caractereNum = '12';
            string nome = "Joao";

            // Tipos lógicos
            bool ligado = true;
            bool desligado = 0;

            // Constantes
            const int preco = 230;
            const int desconto = 10;

            // Inserindo dados no console
            string entrada1 = Console.ReadLine();
            // Convertendo para int
            int entrada2 = Convert.ToInt32(Console.ReadLine());
            // Convertendo para double
            double entrada3 = Convert.Todouble(Console.ReadLine());

    
            // Exibindo dados no console
            Console.WriteLine(inteiro);
            Console.WriteLine(palavra);

        }
    }
}